# Explain the exact missing-product-fact conflict before selecting a fix

Issue #228 remains open. The verified 0.1.61 app with PR #233 Pages now shows an actual `VALIDATION_FAILED` response from `patchListingsItemPreview`: ERROR 90220, `contains_liquid_contents`, `MISSING_ATTRIBUTE`. This confirms an identified missing fact, but neither raw upstream HTTP/metadata nor the source fact/PTD projection has been observed. Existing tests demonstrate that a complete existing false fact is intentionally filtered from fill-only fields and returns the same unhelpful error.

At the existing main Preview seam, expose bounded, fixed explanations for the actual resolver/projection branch instead of silently returning only the original missing message. Distinguish unsupported response metadata/status, an undeclared PTD field, an existing complete value, a managed/protected attribute, and unsupported/readonly field structure. Preserve safe original issue identifiers, operation and request ID using the current error DTO. Do not include raw schema, product values, private binding hashes, credentials or response JSON; do not change field authority, retries, native approval, the durable ledger or formal write behavior.

Use production-seam regressions and both independent reviews. A separately identified, reversible local diagnostic candidate may establish the exact live branch while preserving the verified installed app and vault backup; it is not an official release or CI artifact. Only after that evidence, implement and verify the actual correction and publish a new desktop version. The original missing-fact acceptance stays pending until a usable field or precise supported recovery path has been validated. No formal Amazon mutation is authorized by this diagnosis.

Review identified a contradictory receipt edge: `VALID` with a well-formed raw `ERROR` could lose its blocking issue during public-message privacy filtering. The variation Preview receipt now rejects that contradiction without exposing the private message or changing the global issue normalizer. Unknown receipts remain unknown; missing and non-missing negative cases prove no preview ticket, native approval, or commit.
