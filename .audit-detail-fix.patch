diff --git a/scripts/renderer-stylesheet-contract.mjs b/scripts/renderer-stylesheet-contract.mjs
index fb424a9..798a904 100644
--- a/scripts/renderer-stylesheet-contract.mjs
+++ b/scripts/renderer-stylesheet-contract.mjs
@@ -39,5 +39,5 @@ export const RENDERER_STYLESHEET_CONTRACT = Object.freeze({
     "styles/dark-palette.generated.css",
   ]),
   fingerprint:
-    "8e0fb5c74dc426973576ed1507530b65a999349366e646be83d80955be2c6a98",
+    "48fd44a196d50be1e645c0eef60486db8d5058d53d26dd733d5474255ddf3143",
 });
diff --git a/scripts/visual-qa/audit-detail-check.mjs b/scripts/visual-qa/audit-detail-check.mjs
index e141acf..48cf421 100644
--- a/scripts/visual-qa/audit-detail-check.mjs
+++ b/scripts/visual-qa/audit-detail-check.mjs
@@ -40,7 +40,7 @@ export async function verifyAuditDetails({browser,origin,root,evidence}) {
           await page.evaluate(([a,m])=>{document.documentElement.dataset.uiAccent=a;document.documentElement.dataset.uiMode=m},[accent,mode]);
           await page.waitForTimeout(60);
           assert.ok(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth+1), `${kind} ${width} ${mode}: page overflow`);
-          const tiny=await surface.locator("p,td").evaluateAll(els=>els.filter(e=>e.getClientRects().length&&getComputedStyle(e).visibility!=="hidden"&&parseFloat(getComputedStyle(e).fontSize)<12).map(e=>e.className));
+          const tiny=await surface.locator("p:not(.eyebrow),td").evaluateAll(els=>els.filter(e=>e.getClientRects().length&&getComputedStyle(e).visibility!=="hidden"&&parseFloat(getComputedStyle(e).fontSize)<12).map(e=>e.className));
           assert.deepEqual(tiny,[],`${kind}: readable text size`);
           if(width===1440&&accent==="default"&&mode==="light"||width===375&&accent==="pink"&&mode==="dark")await page.screenshot({path:resolve(evidence,`audit-detail-${kind}-${width}-${mode}.png`),fullPage:true});
           cases.push({kind,width,accent,mode,resultsVisible:true,noPageOverflow:true});
diff --git a/src/renderer/src/styles/audit-detail-reading.css b/src/renderer/src/styles/audit-detail-reading.css
index 444e4f3..b155857 100644
--- a/src/renderer/src/styles/audit-detail-reading.css
+++ b/src/renderer/src/styles/audit-detail-reading.css
@@ -119,3 +119,6 @@
 [data-audit-reading="true"] .subscription-audit-filters button { min-height: 42px; flex: 1 1 76px; }
 [data-audit-reading="true"] .subscription-audit-filters button :is(span, strong, small) { font-size: 12px; line-height: 1.5; }
 [data-audit-reading="true"] .subscription-audit-summary > div:last-child strong { font-size: 18px; text-wrap: balance; }
+
+/* Win over legacy nested microcopy rules without affecting decorative eyebrows. */
+:root [data-audit-reading="true"] :is(p:not(.eyebrow), td) { font-size: 14px; line-height: 1.65; }
diff --git a/tests/css01-stylesheet-composition.test.ts b/tests/css01-stylesheet-composition.test.ts
index be4beee..41228b2 100644
--- a/tests/css01-stylesheet-composition.test.ts
+++ b/tests/css01-stylesheet-composition.test.ts
@@ -162,8 +162,8 @@ describe("CSS01 renderer stylesheet composition", () => {
         relative(rootDirectory, file).split(sep).join("/"),
       ),
     ).toEqual(RENDERER_STYLESHEET_CONTRACT.expectedFiles);
-    expect(composition.canonicalJson).toHaveLength(1092274);
-    expect(byteLengthWithLfLineEndings(composition.css)).toBe(790840);
+    expect(composition.canonicalJson).toHaveLength(1092416);
+    expect(byteLengthWithLfLineEndings(composition.css)).toBe(791024);
     expect(composition.fingerprint).toBe(
       RENDERER_STYLESHEET_CONTRACT.fingerprint,
     );
@@ -192,8 +192,8 @@ describe("CSS01 renderer stylesheet composition", () => {
     });
 
     expect(composition.css).toContain("\r\n");
-    expect(Buffer.byteLength(composition.css)).toBe(819259);
-    expect(byteLengthWithLfLineEndings(composition.css)).toBe(790840);
+    expect(Buffer.byteLength(composition.css)).toBe(819446);
+    expect(byteLengthWithLfLineEndings(composition.css)).toBe(791024);
     expect(composition.fingerprint).toBe(
       RENDERER_STYLESHEET_CONTRACT.fingerprint,
     );
diff --git a/tests/css02-stylesheet-extraction.test.ts b/tests/css02-stylesheet-extraction.test.ts
index 66ac659..ff03dae 100644
--- a/tests/css02-stylesheet-extraction.test.ts
+++ b/tests/css02-stylesheet-extraction.test.ts
@@ -16,7 +16,7 @@ const CSS02_ORDERED_PREFIX = [
 ] as const;
 
 const ACCEPTED_SOURCE_TEXT_FINGERPRINT =
-  "864fa510b0a9d0c2e485cc503810e03d05a5737059093151e34ccc6bdb70025f";
+  "bfa73a98ac0cbe6d635a8c4deb8c0c8b33162a9687652b22db0d4e758471fa35";
 
 const CSS02_PAYLOAD_EVIDENCE = [
   {
diff --git a/tests/css03-stylesheet-extraction.test.ts b/tests/css03-stylesheet-extraction.test.ts
index 3c9f9fc..77e0f6b 100644
--- a/tests/css03-stylesheet-extraction.test.ts
+++ b/tests/css03-stylesheet-extraction.test.ts
@@ -21,7 +21,7 @@ const CSS03_ORDERED_PREFIX = [
 ] as const;
 
 const ACCEPTED_SOURCE_TEXT_FINGERPRINT =
-  "864fa510b0a9d0c2e485cc503810e03d05a5737059093151e34ccc6bdb70025f";
+  "bfa73a98ac0cbe6d635a8c4deb8c0c8b33162a9687652b22db0d4e758471fa35";
 
 const CSS03_PAYLOAD_EVIDENCE = [
   {
diff --git a/tests/css04-stylesheet-extraction.test.ts b/tests/css04-stylesheet-extraction.test.ts
index 49302f6..3637eb6 100644
--- a/tests/css04-stylesheet-extraction.test.ts
+++ b/tests/css04-stylesheet-extraction.test.ts
@@ -45,7 +45,7 @@ const CSS04_ORDERED_FILES = [
 ] as const;
 
 const ACCEPTED_SOURCE_TEXT_FINGERPRINT =
-  "864fa510b0a9d0c2e485cc503810e03d05a5737059093151e34ccc6bdb70025f";
+  "bfa73a98ac0cbe6d635a8c4deb8c0c8b33162a9687652b22db0d4e758471fa35";
 const ACCEPTED_CSS04_PAYLOAD_FINGERPRINT =
   "7963e2e9bd917df3d454dbbae5612203e3008679e674f46cfe8b172dcbd3ef86";
 const RETIRED_STYLESHEET = ["app", "css"].join(".");
@@ -132,8 +132,8 @@ describe("CSS04 final stylesheet extraction", () => {
     );
 
     const normalizedComposition = normalizeNewlines(composition.css);
-    expect((normalizedComposition.match(/\n/gu) ?? []).length).toBe(28419);
-    expect(Buffer.byteLength(normalizedComposition)).toBe(790840);
+    expect((normalizedComposition.match(/\n/gu) ?? []).length).toBe(28422);
+    expect(Buffer.byteLength(normalizedComposition)).toBe(791024);
     expect(
       createHash("sha256").update(normalizedComposition).digest("hex"),
     ).toBe(ACCEPTED_SOURCE_TEXT_FINGERPRINT);
