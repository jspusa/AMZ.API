# Complete variation recovery with read-only canonical evidence

The 0.1.62 live case reached a valid preserved-fact Preview. After external user interaction, the app reported an accepted attach awaiting canonical verification. The existing “重新讀取 Amazon 狀態” control subsequently displayed the intended parent but deliberately retained the uncertain state: it only reads `/variation-family` and never reconciles the durable intent. A displayed parent does not prove complete family membership, dimensions, selectors or preserved facts. No further mutation may be used to resolve this state.

Provide a narrow, main-owned GET recovery operation for the current account/mode/marketplace and exact Seller SKU. It must use the existing canonical reader and `reconcileVariationMoveWrite` predicate, then inspect the durable variation intent through Write Gate. Return a bounded public result distinguishing verified, pending/unknown and no matching operation. The current parent may be shown as an observation, never promoted into verified proof. Do not expose ledger keys, account scope, fingerprints, raw evidence or arbitrary transport. Do not create a Preview ticket, ask for native approval, issue any Preview/PATCH, or retry a submission.

Choose the relevant latest durable intent conservatively. A newer or ambiguous unresolved operation must not be hidden by an older successful receipt. Completed receipts must still match the fresh canonical observation; changed or incomplete source/target identity, FBA, theme, dimensions, selectors, membership, supplemented facts or preserved whole-attribute proof remain unverified. Scope/context must be checked before and after asynchronous reads and persistence. No matching evidence, legacy evidence that cannot prove the current observation, and unavailable inspection fail closed without clearing a write block.

Wire the existing recovery control to this GET operation. Show completed status only from a strictly parsed main-produced verified receipt for the exact displayed SKU, action and target/source relationship. On verified attach show success and keep the already sent intent unusable; on verified detach allow planning a later attach, which still requires its own fresh Preview and native confirmation. Pending, unknown, malformed, mismatched and failed responses retain the block. Keep the current selection, avoid losing a user's draft, and guard late responses after account, marketplace, source or target changes. Recovery must also be discoverable after an app/workspace reopen for a SKU whose durable work is still pending; do not silently enable a replacement submission.

Reproduce the existing button's inability to show completed recovery. Test the public GET seam against real durable pending/unknown evidence and canonical snapshots, including preserved facts, target/membership mismatch, stale/ambiguous receipts, context drift and restart. Prove zero Preview, native approval and PATCH calls in recovery. Test renderer verified/pending transitions, exact identity/target checks, late-response rejection and no resubmission. Run both independent review axes, repository checks and production audit. This main capability ships as 0.1.63; installed 0.1.62, its live Preview and observed accepted request remain separate evidence. Preserve the current userData and durable pending record during installation. Use that existing record for live GET recovery acceptance; do not create another Amazon write for testing.
