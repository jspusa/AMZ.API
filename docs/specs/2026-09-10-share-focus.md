# Share chart focus and pink controls

User-approved renderer-only refinement against main `b4ab9c3df65d2b4440885b6e236f23deacfe48dd`.

- Restore a recognisable 136–192 px pie without expanding the sales plot. Stable brand/category order and zero rows remain.
- At rest, share legends show only their name and percentage. Full currency, SKU count and units appear in an anchored tooltip on hover, keyboard focus or tap. Escape, leaving the region and outside interaction can dismiss it. Snapshot/view changes clear stale detail state. No requests or persistence are introduced.
- Remove the always-visible incomplete-day paragraph. Keep dashed partial segments, point-level realtime labels, table evidence and accessible date/time-zone description.
- Pink sync, date-range controls and brand/category controls share explicit light/dark pink surfaces, borders, selected states and focus. Keep disabled gates, neutral dark canvas and golden sales line.
- No changes to API transport, source calculations, credentials, package version, dependencies, installers, main/preload or operational storage.

Verify source/build stylesheet parity, existing functional regressions, long tooltip amounts, hover/focus/touch dismissals, zero rows, both views, compact plot geometry and all four themes. Synthetic UI validation must not be described as live Amazon/device verification.
